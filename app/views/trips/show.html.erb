<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @trip.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @trip.description %>
</p>

<p>
  <strong>User:</strong>
  <%= @trip.user %>
</p>

 <% if @trip.locations == 0%>
    <%= button_to "Add A Location", new_trip_location_path(@trip), method: :get, class: "ui button" %>
  <% else%>
  <% end%>

<%= link_to  "Locations", trip_locations_path(@trip.id) %>
<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
